import { TextsmsTwoTone } from "@material-ui/icons";
import firebase from "firebase/app";
import firebaseConfig from "../config/firebase";

test("initialize firebase", () => {
  // firebase.initializeApp(firebaseConfig);
});
